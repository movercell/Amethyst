const float PI = 3.14159265359;

float LinearizeDepth(float depth, float near, float far) {
    float ndc = (1.0f - depth) * 2.0 - 1.0;
    float linearDepth = (2.0 * near * far) / (far + near - ndc * (far - near));
    return linearDepth / far;
}